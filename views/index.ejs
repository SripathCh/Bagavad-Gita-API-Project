<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Bhagavad Gita Chapter Lookup</title>
  <link href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <div class="container mt-5">
    <h1 class="text-center">Bhagavad Gita Chapter Lookup</h1>
    <form action="/getChapter" method="POST" class="mt-4">
      <div class="form-group">
        <label for="chapter">Enter Chapter Number</label>
        <input type="number" class="form-control" id="chapter" name="chapter" required>
      </div>
      <button type="submit" class="btn btn-primary">Submit</button>
    </form>
    <div class="mt-4">
      <% if (chapterData) { %>
        <h2>Chapter <%= chapterData.chapter_number %>: <%= chapterData.name_translated %></h2>
        <h3><%= chapterData.name_meaning %></h3>
        <p><%= chapterData.chapter_summary %></p>
        <hr>
        <h4>Summary in Hindi</h4>
        <p><%= chapterData.chapter_summary_hindi %></p>
      <% } %>
      <% if (error) { %>
        <div class="alert alert-danger" role="alert">
          <%= error %>
        </div>
      <% } %>
    </div>
  </div>
  <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.4/dist/umd/popper.min.js"></script>
  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
